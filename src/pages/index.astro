---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const normalizeEntries = (entries: any[], prefix: string) =>
  entries.map(entry => {
    const urlSlug = entry.data.slug ?? entry.id;
    return {
      href: `/articles/${prefix}/${urlSlug}`,
      slug: urlSlug,
      title: entry.data.title,
      description: entry.data.description,
      date: entry.data.pubDate ?? '',
      image: entry.data.image ?? '',
      category: entry.data.category ?? '',
    };
  });

const [agaEntries, edEntries, mensHealthEntries, clinicEntries] = await Promise.all([
  getCollection('aga'),
  getCollection('ed'),
  getCollection('mens-health'),
  getCollection('clinic'),
]);

const edArticles     = normalizeEntries(edEntries, 'ed').sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime());
const agaArticles    = normalizeEntries(agaEntries, 'aga').sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime());
const clinicArticles = normalizeEntries(clinicEntries, 'clinic').sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime());
const mensArticles   = normalizeEntries(mensHealthEntries, 'mens-health').sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime());

const categoryGroups = [
  { key: 'ed',          label: 'ED治療薬ガイド',  emoji: '💊', href: '/articles/ed',          articles: edArticles },
  { key: 'aga',         label: 'AGA治療薬ガイド', emoji: '💇', href: '/articles/aga',         articles: agaArticles },
  { key: 'clinic',      label: 'クリニック比較',   emoji: '🏥', href: '/articles/clinic',      articles: clinicArticles },
  { key: 'mens-health', label: '男性の悩み',       emoji: '💪', href: '/articles/mens-health', articles: mensArticles },
];
---

<Layout title="男の処方箋〜AGA・EDを薬剤師が全部答えてみた〜" description="調剤薬局長として18年のキャリアを持つ現役薬剤師がED・AGA治療薬・オンラインクリニックを本音で比較・解説します。">
  <main class="container page-wrapper py-8">

    <!-- ① ヒーローエリア -->
    <section class="mb-16 px-2">
      <div class="hero-image-wrapper mb-8">
        <img
          src="/hero.webp"
          alt="男の処方箋 クリニックに忖度しない薬剤師が本音で解説"
          class="w-full rounded-2xl object-cover"
          loading="eager"
        />
      </div>
      <h1 class="text-base font-black text-slate-800 text-center leading-snug mb-8">
        クリニックに忖度しない薬剤師が本音で解説
      </h1>
      <div class="grid grid-cols-2 gap-5">
        <!-- EDボタン：sky-500ボーダー、三日月SVG -->
        <a href="#ed-section" class="hero-icon-btn hero-icon-btn--ed">
          <svg viewBox="0 0 32 32" width="40" height="40" fill="none" aria-hidden="true">
            <path d="M22 16a10 10 0 1 1-10-10 8 8 0 0 0 10 10z"
              stroke="#0ea5e9" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span class="hero-icon-btn__label">夜の悩み・ED治療</span>
        </a>
        <!-- AGAボタン：blue-600ボーダー、髪が抜けるSVG -->
        <a href="#aga-section" class="hero-icon-btn hero-icon-btn--aga">
          <svg viewBox="0 0 32 32" width="40" height="40" fill="none" aria-hidden="true">
            <circle cx="16" cy="20" r="9" stroke="#2563eb" stroke-width="2" stroke-linecap="round"/>
            <line x1="11" y1="12" x2="8"  y2="4" stroke="#2563eb" stroke-width="2" stroke-linecap="round"/>
            <line x1="16" y1="11" x2="16" y2="3" stroke="#2563eb" stroke-width="2" stroke-linecap="round"/>
            <line x1="21" y1="12" x2="24" y2="4" stroke="#2563eb" stroke-width="2" stroke-linecap="round"/>
          </svg>
          <span class="hero-icon-btn__label">AGA・薄毛の悩み</span>
        </a>
      </div>
    </section>

    <!-- ② 信頼バッジ -->
    <section class="mb-16 px-2">
      <div class="grid grid-cols-3 gap-3">
        <!-- 盾＋×：クリニックに忖度しない -->
        <div class="trust-badge">
          <svg viewBox="0 0 32 32" width="32" height="32" fill="none" stroke="#0284c7" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M16 3L4 8v7c0 6 5 11 12 13 7-2 12-7 12-13V8L16 3z"/>
            <line x1="12" y1="13" x2="20" y2="21"/>
            <line x1="20" y1="13" x2="12" y2="21"/>
          </svg>
          <span class="trust-badge__label">クリニックに<br/>忖度しない</span>
        </div>
        <!-- 書類：添付文書・論文ベース -->
        <div class="trust-badge">
          <svg viewBox="0 0 32 32" width="32" height="32" fill="none" stroke="#0284c7" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M7 3h13l6 6v20H7V3z"/>
            <polyline points="20 3 20 9 26 9"/>
            <line x1="11" y1="16" x2="22" y2="16"/>
            <line x1="11" y1="21" x2="17" y2="21"/>
          </svg>
          <span class="trust-badge__label">添付文書・<br/>論文ベース</span>
        </div>
        <!-- 吹き出し：現役薬剤師が本音解説 -->
        <div class="trust-badge">
          <svg viewBox="0 0 32 32" width="32" height="32" fill="none" stroke="#0284c7" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M4 5h24a2 2 0 0 1 2 2v11a2 2 0 0 1-2 2H10l-6 6v-6H4a2 2 0 0 1-2-2V7a2 2 0 0 1 2-2z"/>
          </svg>
          <span class="trust-badge__label">現役薬剤師が<br/>本音解説</span>
        </div>
      </div>
    </section>

    <!-- ③ クリニック比較バナー（2分割） -->
    <section class="mb-16 px-2">
      <div class="grid grid-cols-2 gap-3">
        <a href="/articles/ed/ed-clinic-comparison" class="clinic-card">
          <p class="clinic-card__title">💊 ED治療<br/>クリニック比較</p>
          <p class="clinic-card__sub">DMM・レバクリ・クリニックフォア</p>
          <span class="clinic-card__btn">比較を見る →</span>
        </a>
        <a href="/articles/aga/aga-clinic-comparison" class="clinic-card">
          <p class="clinic-card__title">💇 AGA治療<br/>クリニック比較</p>
          <p class="clinic-card__sub">DMM・レバクリ・クリニックフォア</p>
          <span class="clinic-card__btn">比較を見る →</span>
        </a>
      </div>
    </section>

    <!-- ④ カテゴリ別記事グリッド -->
    {categoryGroups.map(group => group.articles.length > 0 && (
      <section id={group.key === 'ed' ? 'ed-section' : group.key === 'aga' ? 'aga-section' : undefined} class="mb-20 px-2">
        <div class="flex justify-between items-center mb-4">
          <h2 class="text-base font-black text-slate-800 flex items-center gap-2">
            <span class="w-1.5 h-5 bg-sky-500 rounded-full flex-shrink-0"></span>
            {group.emoji} {group.label}
          </h2>
          <a href={group.href} class="category-list-btn">
            <span>記事一覧をみる</span>
            <svg viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="9 18 15 12 9 6"/>
            </svg>
          </a>
        </div>
        <div class="articles-grid">
          {group.articles.slice(0, 4).map(article => (
            <a href={article.href} class="article-card">
              <div class="article-card-image">
                {article.image
                  ? <img src={article.image} alt={article.title} loading="lazy" />
                  : <div class="article-card-image-placeholder"></div>
                }
              </div>
              <div class="article-card-body">
                {article.category && (
                  <span class="article-category-badge">{article.category}</span>
                )}
                <h3 class="article-card-title">{article.title}</h3>
                <p class="article-card-desc">{article.description}</p>
              </div>
            </a>
          ))}
        </div>
      </section>
    ))}

    <!-- ⑤ 運営者紹介 -->
    <section class="mb-8 px-2">
      <div class="bg-slate-50 border border-slate-200 rounded-2xl p-6">
        <p class="text-sm text-slate-600 font-medium leading-relaxed mb-4">
          調剤薬局長として18年、ED・AGA処方箋を日常的に扱う現役薬剤師が運営しています。
        </p>
        <a href="/about" class="text-sm font-bold text-sky-600 hover:underline">
          運営者詳細を見る →
        </a>
      </div>
    </section>

  </main>
</Layout>

<style>
  .page-wrapper {
    max-width: 480px;
    margin: 0 auto;
  }

  /* Hero */
  .hero-image-wrapper {
    width: 100%;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    border-radius: 1rem;
    background: #f8fafc;
  }
  .hero-image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* Hero icon buttons */
  .hero-icon-btn {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.6rem;
    padding: 1.4rem 0.75rem;
    border-radius: 1rem;
    background: #ffffff;
    text-decoration: none;
    box-shadow: 0 2px 8px rgba(0,0,0,0.06);
    transition: background 0.2s, transform 0.15s;
    text-align: center;
  }
  .hero-icon-btn:active { transform: scale(0.97); }

  .hero-icon-btn--ed  { border: 2px solid #0ea5e9; }
  .hero-icon-btn--ed:hover  { background: #f0f9ff; }
  .hero-icon-btn--aga { border: 2px solid #2563eb; }
  .hero-icon-btn--aga:hover { background: #eff6ff; }

  .hero-icon-btn__label {
    font-size: 0.78rem;
    font-weight: 900;
    color: #1e293b;
    line-height: 1.3;
  }

  /* Trust badges */
  .trust-badge {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    padding: 1.1rem 0.5rem;
    background: #f0f9ff;
    border: 1px solid #bae6fd;
    border-radius: 0.875rem;
    text-align: center;
  }
  .trust-badge__label {
    font-size: 0.65rem;
    font-weight: 700;
    color: #334155;
    line-height: 1.4;
  }

  /* Clinic cards (2-panel) */
  .clinic-card {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.5rem;
    padding: 1.1rem 1rem;
    border-radius: 1rem;
    background: linear-gradient(135deg, #0f2d5a 0%, #1a4280 60%, #0e7490 100%);
    text-decoration: none;
    transition: opacity 0.2s;
  }
  .clinic-card:hover { opacity: 0.92; }
  .clinic-card__title {
    font-size: 0.8rem;
    font-weight: 900;
    color: #ffffff;
    line-height: 1.45;
  }
  .clinic-card__sub {
    font-size: 0.6rem;
    font-weight: 700;
    color: #bae6fd;
    line-height: 1.4;
  }
  .clinic-card__btn {
    margin-top: 0.4rem;
    font-size: 0.68rem;
    font-weight: 800;
    color: #0f2d5a;
    background: #ffffff;
    padding: 0.3rem 0.7rem;
    border-radius: 9999px;
    white-space: nowrap;
  }

  /* Category section list button */
  .category-list-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    font-size: 0.72rem;
    font-weight: 700;
    color: #0369a1;
    background: #f0f9ff;
    border: 1.5px solid #7dd3fc;
    border-radius: 9999px;
    padding: 0.3rem 0.75rem;
    text-decoration: none;
    white-space: nowrap;
    transition: background 0.2s;
  }
  .category-list-btn:hover { background: #e0f2fe; }

  /* Article card grid */
  .articles-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    margin-top: 1rem;
  }

  .article-card {
    display: flex;
    flex-direction: column;
    background: #fff;
    border-radius: 14px;
    border: 1px solid #f1f5f9;
    box-shadow: 0 2px 8px rgba(0,0,0,0.03);
    text-decoration: none;
    overflow: hidden;
    transition: transform 0.2s, box-shadow 0.2s;
  }
  .article-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 20px rgba(0,0,0,0.07);
    border-color: #e0f2fe;
  }

  .article-card-image {
    width: 100%;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    flex-shrink: 0;
    background: #f8fafc;
  }
  .article-card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .article-card-image-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #e0f2fe, #f0f9ff);
  }

  .article-card-body {
    flex: 1;
    padding: 0.6rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .article-category-badge {
    font-size: 0.6rem;
    font-weight: 800;
    color: #0369a1;
    background: #f0f9ff;
    padding: 2px 6px;
    border-radius: 9999px;
    align-self: flex-start;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: 100%;
  }

  .article-card-title {
    font-size: 0.72rem;
    font-weight: 900;
    color: #1e293b;
    line-height: 1.4;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
    margin: 0;
  }

  .article-card-desc {
    font-size: 0.62rem;
    color: #64748b;
    line-height: 1.5;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    margin: 0;
  }
</style>
